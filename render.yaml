services:
  - type: web
    name: small-magellanic-cloud
    env: node
    plan: free
    buildCommand: npm install && python3 -m pip install --user -r tools/study/requirements.txt
    startCommand: node server.mjs
    autoDeploy: true
