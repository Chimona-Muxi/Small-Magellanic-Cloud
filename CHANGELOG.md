# 更新日志

## 2026-07-01

- 精简首页、游戏页、棋类页的重复信息：去掉重复标题、重复面包屑、卡片标签和卡片底部的“打开”。
- 统一设置面板交互：语言和外观改为点击右侧当前值区域触发，左侧说明只负责展示。
- 设置面板改为可扩展的主列表加二级选择页结构，避免后续设置项变多后撑乱页面。
- 提升设置面板层级与可读性：弹层背景改为接近不透明，避免与游戏侧栏和棋盘内容重叠时发虚。
- 统一桌面端和移动端的页眉/设置入口，保留必要导航，减少当前页重复标识。
- 三个棋类游戏页去掉“棋类系统”语义，统一为更轻的“棋类”。
- 三个联机游戏继续使用 SSE 实时同步，并在服务器端加入响应头立即 flush 与心跳保活，降低移动端或代理断开长连接造成的同步延迟。
